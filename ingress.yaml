apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: restapi-ingress
spec:
  rules:
  - host: restapi.com 
    http:
      paths:
      - path: /
        backend:
          service:
            name: restapi
            port:
              number: 80